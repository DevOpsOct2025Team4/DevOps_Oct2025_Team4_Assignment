name: Full CI

on:
  pull_request:
    branches: [develop, main]
  push:
    branches: [main]

jobs:
  full_ci:
    name: Common CI
    uses: ./.github/workflows/Common-CI.yml
    with:
      upload_client_artifact: true
